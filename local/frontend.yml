apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: stand
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: fraguinha/stand-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: stand
spec:
  selector:
    app: frontend
  ports:
    - port: 8080
      targetPort: 8080
